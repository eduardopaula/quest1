CREATE OR REPLACE FUNCTION HR.OW_CHECK_CUST (C_ID NUMBER)
RETURN NUMBER
AS 
I NUMBER;
BEGIN


DBMS_OUTPUT.PUT_LINE('{ HR}[1] I = ' || I);
SELECT 1 INTO I 
FROM HR.EMPLOYEES A
WHERE A.EMPLOYEE_ID = C_ID;
RETURN (1);
EXCEPTION WHEN OTHERS THEN RETURN (0);
END;
/
